# Frontend app for multilingual transcription

In this subfolder, we keep the code for client app, written in React.js.

The frontend app communicates with backend via REST API.

## Setup

```shell
npm install
```

Run locally for development:

```shell
npm run dev
```
